{
  "name": "mi-app-de-notas-web-root",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm:dev --prefix frontend\" \"npm:start --prefix backend\"",
    "build": "npm install --prefix frontend && npm run build --prefix frontend && npm install --prefix backend",
    "start": "npm start --prefix backend"
  },
  "dependencies": {
    "nodemailer": "^7.0.6"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}